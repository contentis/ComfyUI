# Install Comfy Quantizer Ext

Prerequisite
```
# Optional set CTK path
export CUDA_HOME

# Build requirements
sudo apt-get install python-dev-is-python3
```

```
cd kernel
pip install -e . --no-build-isolation -vvv
```